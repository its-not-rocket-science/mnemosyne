# GitHub repo metadata

## Description

Spaced-repetition language learning from any text. Parse → drill → review with FSRS. FastAPI + spaCy + accessible vanilla-JS frontend. Spanish MVP; plugin interface for additional languages.

_(160 characters max — this is 159.)_

## Topics

```
language-learning
spaced-repetition
fsrs
spacy
fastapi
postgresql
redis
web-components
accessibility
python
vanilla-js
```

## Notes

- The current `pyproject.toml` lists `authors = ["OpenAI <support@openai.com>"]` — update this to the actual maintainer before the repo goes public.
- The repo name `mnemosyne` is taken on PyPI; the package is not published there, so no conflict, but worth noting if that changes.
- Consider adding a `LICENSE` file. The repo has none currently.
