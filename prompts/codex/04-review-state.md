# Codex Prompt 4 — Review State

Persist review state using SQLAlchemy.

Requirements:
- pure scheduler logic must remain separate
- route returns existing response shape
- JSON-serialisable state

Output models, route changes, and tests.
