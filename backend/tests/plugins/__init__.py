# Test-support plugin stubs.
# These are spaCy-free implementations of the LanguagePlugin protocol used
# as test doubles in unit and integration tests.  None of them have a
# create_plugin() factory and none are registered by load_plugins().
